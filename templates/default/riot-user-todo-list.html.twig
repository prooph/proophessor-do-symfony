<div class="col-md-12">
    <p if={_.isEmpty(todos)} class="alert alert-warning">
        <strong>The Todo list looks empty. Do you want to create a
            <a href="{ userTodoFormLink }">new Todo</a>?</strong>
    </p>
    <div if={ !_.isEmpty(todos)} class="row">
        <div class="col-md-12 text-right">
            <a href="{ userTodoFormLink }" class="btn btn-success">new Todo</a>
        </div>
    </div>
    <p></p>
    <div class="row">
        <div class="col-md-12">
            <div class="list-group">
                <user-todo each="{ todos }" app="{ context.app }"></user-todo>
            </div>
        </div>
    </div>
</div>
<script type="text/javascript">
    function (context) {

        var self = this;

        self.context = context;
        self.userTodoFormLink = "{{ path('page::user-todo-form', {'userId': '00000000-0000-0000-0000-000000000000'}) }}".replace('00000000-0000-0000-0000-000000000000', context.app.userId);
        console.log(context.app.userId);
        self.todos = context.app.TodoStore.getAll();
    }
</script>
